import io;

class Fac {

    int[] a;
    public int ComputeFac(int num){
        int num_aux ;

        if (num < a.length)
            num_aux = 2;
        else

            num_aux = a.length;
        a[0] = 1;
        return num_aux;
    }
    public static void main(String[]args){

        io.println(new Fac().ComputeFac(10));//assuming the existence // of the classfile io.class

        int aux;
        a = io.println();
        aux = this.ComputeFac(a);
    }
}