import io;
class Fac {
    int[] a;
    public int compFac(int num){
        int num_aux;
        int i;
        a = new int[10];
        if (num < a[0])
            a[0] = 1 + i;
        else
            num_aux = num;

        i = num_aux + this.compFac(num_aux - 1);
        return num_aux;
    }

    public static void main(String[]args){
        io.println(new Fac().compFac(10));
    }
}